#ifndef SYSSETUP_h
#define SYSSETUP_h
#include <stdint.h>

/** Init the platform */
void platform_init();

/** Enable SysTick */
void enable_tick ();

/** Configure the GPIOs */
void configure_gpio();

#endif
